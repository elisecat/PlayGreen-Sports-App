import { styled } from "styled-components";

export const Button = styled.button`
  background: rgba(34, 34, 67, 1); 
  border: none;
  border-radius: 12px;
  cursor: pointer;
  position: absolute;
  top: 10px; 
  left: 10px;
  padding: 1rem;
  z-index: 1;
`;